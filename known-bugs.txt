-The ant targets do not work if their is a space anywhere in your project path.
- String concatenation where one of the string has FlowSink.ANY gives the wrong type.  See tests/flow/StringConcat.java for an example
